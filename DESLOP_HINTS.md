# Deslop hints

Manually identified refactor targets for `/deslop` to pick up once metric-driven
candidates (priority 1–3 in the skill) are exhausted. Mark completed with `[x]`.

<!-- Add entries like:
- [ ] Extract <helper> in `Sources/EngramCore/<File>.swift` — <pattern repeated N times>
-->
